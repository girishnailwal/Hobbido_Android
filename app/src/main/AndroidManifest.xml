<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hobbido.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hobbido_and">
       <!-- <activity android:name=".activity.SucessUpdateActivity"></activity>-->
        <!-- <activity android:name=".activity.LoginActivity"></activity> -->
        <!-- <activity android:name=".activity.VerificationActivity"></activity> -->
        <!-- <activity android:name=".activity.CategoryActivity"></activity> -->
        <activity android:name=".activity.ProfileSetUpActivity" /> <!-- <activity android:name=".activity.SignupActivity"></activity> -->
        <!-- <activity android:name=".activity.LanguageActivity"></activity> -->
        <activity android:name=".activity.SucessUpdateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>